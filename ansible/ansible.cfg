[defaults]
# Keep role discovery deterministic for local runs, CI, and break-glass ISO bootstraps.
roles_path = ./automation-roles:./playbooks/roles:/etc/ansible/roles:/usr/share/ansible/roles
inventory = ./node-inventory.yml
host_key_checking = False
retry_files_enabled = False
interpreter_python = auto_silent
