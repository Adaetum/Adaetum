apiVersion: v1
kind: ConfigMap
metadata:
  name: ansible-cluster-config
data:
  ANSIBLE_RUNNER_IMAGE: 'registry.adaetum.invalid/gitea-admin/ansible-runner:build-67fc36dceb83756a'
  TAILSCALE_DOMAIN: 'example-tailnet.ts.net'
  TAILSCALE_CLUSTER_TAG: 'tag:cluster'
