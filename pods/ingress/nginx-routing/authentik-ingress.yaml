apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: authentik-ui
  namespace: authentik
  annotations:
    external-dns.alpha.kubernetes.io/hostname: authentik.mudazukai.cloud
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  ingressClassName: nginx
  rules:
    - host: authentik.mudazukai.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: authentik-server
                port:
                  number: 80
    - host: authentik.mudazukai.cloud
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: authentik-server
                port:
                  number: 80
